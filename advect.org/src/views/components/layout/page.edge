<!DOCTYPE html>
<html lang="en" hidden>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="prefetch" href="/assets/advect.js" />
  <link rel="prefetch" href="/assets/main.css" />
  <link rel="stylesheet" href="/assets/main.css" />

  @if ($slots.head_assets)
  {{{ await $slots.head_assets() }}}
  @end
    
    @each(meta in meta_fields)
      <meta name="{{meta.name}}" content="{{meta.content}}">
    @end

  <title>{{ title }}</title>
</head>

<body>
  <header>
    @if ($slots.header)
      {{{ await $slots.header() }}}
    @end
  </header>

  <main>
    @if ($slots.content)
    {{{ await $slots.content() }}}
    @end
    <footer>
      @if ($slots.footer)
      {{{ await $slots.footer() }}}
      @end
      @if ($slots.footer_assets)
      {{{ await $slots.footer_assets() }}}
      @end
      <script type="module" async src="/assets/advect.js"></script>
      <script type="module" src="/assets/advect.org.js"></script>
  
    </footer>
  </main>


  </div>
</body>

</html>